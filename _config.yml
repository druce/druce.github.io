# see https://github.com/jekyll/minima/blob/master/_config.yml

# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

# site settings
host: 0.0.0.0
name: Druce Vertes
title: StreetEYE Blog
email: streeteye@streeteye.com
description: >- # this means to ignore newlines until "baseurl:"
  Mad Science About Tech, Markets And Political Economy
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://blog.streeteye.com" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: streeteye
github_username: druce
linkedin_username: drucevertes
show_excerpts: true


# If you want to link only specific pages in your header, uncomment
# this and add the path to the pages in order as they should show up
#header_pages:
# - about.md

# Build settings
theme: minima

# I believe these are the default now, don't need to be set explicitly
# markdown: kramdown
# highlighter: rouge
# kramdown:
#   input: GFM
#   auto_ids: true
#   syntax_highlighter: rouge

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll_picture_tag  
  
google_analytics: UA-24111647-1

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#

# below doesn't seem necessary, no extra items under _site
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
